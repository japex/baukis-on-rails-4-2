<%= markup do |m|
  p = FormPresenter.new(f, self)
  m << p.notes
  p.with_options(required: true) do |q|
    m << q.text_field_block(:application_name, 'アプリケーション名', size: 16)
    m << q.number_field_block(:session_timeout_in_min, 'セッションタイムアウト(分)')
  end
end %>
